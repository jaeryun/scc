{
  "autoMemoryEnabled": true,
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "agent",
            "if": "Bash(git push*)",
            "prompt": "1. Use the Read tool to read the file .claude/skills/doc-sync.md. 2. If the file exists, return {\"ok\": true}. 3. If the file does not exist, return {\"ok\": false, \"reason\": \"doc-sync.md not found\"}.",
            "statusMessage": "테스트: Read 도구 접근 확인...",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
