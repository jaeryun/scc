# reference/ — 참조 자료

여러 뷰에서 공유되는 참조 자료를 담습니다. `reference/`만 유일하게 하위 디렉터리 중첩을 허용합니다.

**로딩 시점:** 필요 시 (API 참조, 데이터 모델 확인, env 변수 조회)

- `api/` — API 엔드포인트 명세, OpenAPI 스펙
- `data-models/` — DB 스키마, 엔티티 관계, 공유 타입
- `env/` — 환경변수 목록, 설정값, 피처 플래그

> 구조와 분류 기준은 `@docs/common/reference/index.md` 참조.
