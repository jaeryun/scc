# patterns/ -- Implementation patterns

Code examples and recipes to reference during implementation.

- `data-patterns.md`      -- React Query (prefetchQuery, useSuspenseQuery), apiClient, service patterns
- `form-patterns.md`      -- TanStack Form + Zod full recipes (basic, Sheet, Multi-step, arrays)
- `theme-patterns.md`     -- How to add a new theme (OKLCH, CSS variables)
- `component-patterns.md` -- Component placement decision tree (components/ vs modules/)
- `first-feature.md`      -- Learn by following IPAM: new feature 7-step workflow
